# State — 2026-09-16

**Branch:** `main`
**Last shipped:** Phase 3 part 6 — meal plan and shopping list screens in two
languages, and the new `core/l10n/date_labels.dart` layer. Implemented and
verified (`make check` clean, 477 Dart tests); not yet committed.
**In flight:** none
**Next:** Phase 3 is complete. Phase 4 (`docs/ROADMAP.md`) — going real —
starts with Part 1, a real Supabase project; Parts 2 (real device) and 3
(Google sign-in) depend on it existing. Phase 5's deferred-feature bullets
wait until Phase 4 ships.
**Latest decision:** D94

Update this file as the last step of closing a slice (`/close-slice`), not
mid-task. If it disagrees with `git log`, trust `git log` and fix this file.
